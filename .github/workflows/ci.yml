name: CI

on:
  push:
  pull_request:

# Avoid duplicate/redundant runs: collapse a branch push and its open PR into
# one concurrency group. PR events expose `github.head_ref` (the source branch);
# push events fall back to `github.ref`, so both map to the same group key.
concurrency:
  group: ci-${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

# Node 24 LTS — current LTS, comfortably above the toolchain's minimums
# (Vite / Vitest / Playwright). Pin here so all jobs share one version.
env:
  NODE_VERSION: '24'

jobs:
  frontend:
    name: Frontend (lint + typecheck + coverage)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - name: Audit frontend dependencies
        run: npm audit --audit-level=high --json > audit-results.json
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run test:coverage
      - name: Upload frontend dependency audit
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dependency-audit
          path: frontend/audit-results.json
          retention-days: 7
          if-no-files-found: error
      - name: Upload frontend coverage
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: frontend-coverage
          path: frontend/coverage/
          retention-days: 7
          # The coverage report is the evidence this job exists to produce, so a
          # missing directory is a broken config, not an acceptable no-op.
          if-no-files-found: error

  backend:
    name: Backend (lint + typecheck + coverage)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: backend/package-lock.json
      - run: npm ci
      - name: Audit backend dependencies
        run: npm audit --audit-level=high --json > audit-results.json
      - run: npm run lint
      - run: npm run typecheck
      - name: Start ephemeral test Postgres
        working-directory: .
        run: |
          docker compose -f docker-compose.test.yml up -d --wait
          docker compose -f docker-compose.test.yml ps
      - name: Run tests with coverage (integration tests hit the ephemeral Postgres)
        env:
          NODE_ENV: test
          PORT: '8080'
          DATABASE_URL: postgres://todo:todo@localhost:5432/todo
          CORS_ORIGIN: http://localhost:5173
        run: npm run test:coverage
      - name: Upload backend dependency audit
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: backend-dependency-audit
          path: backend/audit-results.json
          retention-days: 7
          if-no-files-found: error
      - name: Upload backend coverage
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: backend-coverage
          path: backend/coverage/
          retention-days: 7
          if-no-files-found: error
      - name: Stop ephemeral test Postgres
        if: always()
        working-directory: .
        run: docker compose -f docker-compose.test.yml down -v

  e2e:
    name: E2E (Playwright)
    runs-on: ubuntu-latest
    # This job builds images, health-gates a stack, and stops/starts containers
    # from inside the tests, so cap it rather than fall back to the 6h default.
    timeout-minutes: 30
    defaults:
      run:
        working-directory: e2e
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: e2e/package-lock.json
      - run: npm ci
      - name: Audit E2E dependencies
        run: npm audit --audit-level=high --json > audit-results.json
      - run: npm run lint
      - run: npm run typecheck
      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium
      - name: Build images and bring the stack up health-gated
        working-directory: .
        run: docker compose up -d --build --wait --wait-timeout 180
      - name: Poll /api/health until 200 (proxied through nginx)
        working-directory: .
        run: |
          for i in $(seq 1 30); do
            if curl -fsS http://localhost:8080/api/health > /dev/null; then
              echo "backend healthy via proxy"
              exit 0
            fi
            echo "waiting for /api/health ($i)..."
            sleep 2
          done
          echo "timed out waiting for /api/health"
          exit 1
      - run: npm test
      - name: Capture compose logs on failure
        if: failure()
        working-directory: .
        run: docker compose logs --no-color | tee compose-logs.txt
      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: e2e/playwright-report/
          retention-days: 7
      - name: Upload E2E dependency audit
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-dependency-audit
          path: e2e/audit-results.json
          retention-days: 7
          if-no-files-found: error
      - name: Upload axe accessibility results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: axe-results
          path: e2e/axe-results/
          retention-days: 7
          # Per-state axe JSON is the accessibility gate's evidence; if it is
          # missing the scan never ran, which is a broken job, not a clean pass.
          if-no-files-found: error
      - name: Upload compose logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-compose-logs
          path: compose-logs.txt
          retention-days: 7
          if-no-files-found: warn
      - name: Tear down the stack
        if: always()
        working-directory: .
        run: docker compose down -v

  compose-smoke:
    name: Compose smoke (one-command bring-up)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    # Independent of the frontend/backend/e2e jobs: this proves the PRODUCTION
    # docker-compose.yml stack builds, comes up health-gated, serves the SPA, and
    # reverse-proxies /api through nginx. It must fail the build on any failure.
    steps:
      - uses: actions/checkout@v4
      - name: Build images and bring the stack up health-gated
        # Bounded wait so a container stuck `starting`/unhealthy fails fast
        # instead of blocking until the GitHub job timeout.
        run: docker compose up -d --build --wait --wait-timeout 180
      - name: Poll /api/health until 200 (proxied through nginx)
        run: |
          for i in $(seq 1 30); do
            if curl -fsS http://localhost:8080/api/health > /dev/null; then
              echo "backend healthy via proxy"
              exit 0
            fi
            echo "waiting for /api/health ($i)..."
            sleep 2
          done
          echo "timed out waiting for /api/health"
          exit 1
      - name: Assert SPA is served at /
        run: curl -fsS http://localhost:8080/ > /dev/null
      - name: Assert proxied /api/health returns ok + db up
        run: |
          body=$(curl -fsS http://localhost:8080/api/health)
          echo "Response: $body"
          echo "$body" | grep -q '"status":"ok"'
          echo "$body" | grep -q '"db":"up"'
      - name: Assert proxied /api/todos returns 200 + empty list
        # Proves the empty data path the EmptyState renders works end-to-end
        # through nginx → backend → fresh db. Story 3.1 owns the browser E2E.
        run: |
          body=$(curl -fsS http://localhost:8080/api/todos)
          echo "Response: $body"
          echo "$body" | grep -qx '\[\]'
      - name: Assert backend runtime image security posture
        run: |
          test "$(docker compose exec -T backend id -u)" -ne 0
          docker compose exec -T backend node <<'NODE'
          const { existsSync } = require('node:fs')
          const { join } = require('node:path')
          const { devDependencies = {} } = require('/app/package.json')
          const present = Object.keys(devDependencies).filter((name) =>
            existsSync(join('/app/node_modules', name)),
          )
          if (present.length > 0) {
            console.error(`Direct devDependencies present in runtime image: ${present.join(', ')}`)
            process.exit(1)
          }
          NODE
          docker compose exec -T backend node <<'NODE'
          const { readdirSync } = require('node:fs')
          const { join } = require('node:path')
          const environmentFiles = []
          const scan = (directory) => {
            for (const entry of readdirSync(directory, { withFileTypes: true })) {
              const path = join(directory, entry.name)
              if (entry.isDirectory() && entry.name !== 'node_modules') scan(path)
              if (!entry.isDirectory() && entry.name.startsWith('.env')) environmentFiles.push(path)
            }
          }
          scan('/app')
          if (environmentFiles.length > 0) {
            console.error(`Environment files present in runtime image: ${environmentFiles.join(', ')}`)
            process.exit(1)
          }
          NODE
      - name: Assert no real environment file is tracked
        run: |
          tracked_environment_files="$(
            git ls-files '.env*' '**/.env*' |
              while IFS= read -r file; do
                case "$file" in
                  .env.example) ;;
                  *) printf '%s\n' "$file" ;;
                esac
              done
          )"
          if [ -n "$tracked_environment_files" ]; then
            echo "Tracked environment files are not allowed:"
            echo "$tracked_environment_files"
            exit 1
          fi
      - name: Dump compose logs on failure
        if: failure()
        run: docker compose logs
      - name: Tear down the stack
        if: always()
        run: docker compose down -v
