# Decision Log — Todo App PRD

Canonical memory and audit trail for this PRD run. Every decision, change, and override is recorded here.

## 2026-06-15

- **Intent:** Create. No existing PRD found in `_bmad-output/planning-artifacts/prds/`.
- **Working mode:** Fast path (user-requested).
- **Stakes:** Training exercise (BMAD). Low real-world stakes, but PRD must be implementation-ready to feed downstream UX, architecture, stories, tests, Docker, and QA deliverables.
- **Source input:** User-supplied high-level prose PRD (pasted in chat) for a simple full-stack single-user Todo web app, no auth.
- **Confirmed scope from source:** single individual user; no authentication; CRUD-style todo management (create, view, complete, delete); persistence across refreshes/sessions; frontend + backend + tests + Docker Compose + documentation required as training deliverables.

### Gap-closing batch decisions (Fast path)

- **Tech stack:** React + TypeScript frontend, Node/Express + TypeScript backend. → Recorded in `addendum.md` (PRD stays capability-focused).
- **Persistence:** Dedicated database service (Postgres) as its own Docker Compose container, for best DevOps/Docker training value. → `addendum.md`.
- **Edit todo text:** IN SCOPE for v1. User chose to add editing a todo's description as a functional requirement (expands beyond the original source's create/view/complete/delete list). Logged as an intentional scope expansion.
- **Todo fields:** Minimal — `id`, `description`, `completed` status, `created_at` timestamp. No `updated_at` (user chose minimal despite edit being in scope; edit updates description in place).
- **Downstream depth:** Moderate — concrete acceptance + success criteria and clear NFRs; no enterprise-grade analytics.

### Finalize (2026-06-15)

- **Decision-log audit:** all gap-batch decisions captured in PRD (stack/persistence → addendum; edit → FR-4; minimal fields → Glossary/§13; depth → §7/§8).
- **Input reconciliation:** subagent compared user source prose PRD vs prd.md + addendum.md → `reconcile-source.md`. No core source requirement dropped. Flagged additions (training framing, Docker/tests/docs deliverables, FR-4 editing) confirmed as intentional user decisions. Qualitative "feel" (clear/intuitive/instantaneous/polished) preserved by adding an explicit "Experience qualities" intent clause to NFR-3.
- **Reviewer gate (rubric walker):** `review-rubric.md`. Verdict: strong across 6 dimensions, adequate on downstream usability. 0 critical, 0 high, 1 medium, 3 low.
  - Medium (FIXED): NFR cross-refs cited "§9" but NFRs are in §8 — corrected 3 references.
  - Low (FIXED): Glossary "Created-at" normalized with canonical `created_at`; SM-1 "discoverable" target tightened to a concrete unaided-completion check; NFR-5 split into testable extensibility core vs. intent.
- **Open items triage:** §12 Open Questions (ordering, description length cap, updated_at, concurrency, test levels) — all non-blockers with safe defaults; DEFERRED to downstream owners (architecture / test-design). Revisit condition: confirm before/within bmad-create-architecture and bmad-testarch.
- **Polish:** structure + prose graded strong by rubric; light-touch polish applied in place rather than full editorial subagent passes (training-scope, moderate stakes). Decision logged.
- **External handoffs:** none configured.
- **Close:** prd.md `status: final`, `updated: 2026-06-15`. Artifacts: prd.md, addendum.md, review-rubric.md, reconcile-source.md, .decision-log.md.
